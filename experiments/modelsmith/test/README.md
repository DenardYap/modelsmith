# What this directory does
In this directory I implement a few models to test out the correctness and efficiency of BinaryLinear.


# Pip install 
run pip install -r requirements.txt to download the libraries needed for this directory 